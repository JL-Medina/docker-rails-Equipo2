<p id="notice"><%= notice %></p>

<h1><%=t('name_products')%></h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Precio</th>
      <th>Descripcion</th>
      <th>Categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.price %></td>
        <td><%= product.description %></td>
        <td><%= product.category.name %></td>
        <td><%=number_field_tag :quantity, 0, min: 0, step: 1, id: "quantity_id_#{product.id}"%></td>
        <td><%= link_to 'Add to budget', "#",onclick: "addProduct(#{product.id})" %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('new.new_product'), new_product_path %>

<script>
  function newBudget (){
    


  }
  function addProduct(product_id) {
    var quantity = document.getElementById("quantity_id_" + product_id).value;
    
    if (quantity != 0)
    {
      
    }
    
  }
</script>